"""Single source of truth for the package version.

Version management is tag-driven on GitHub. This version must match the
release tag being cut (``v0.5.0`` — the additive AZ-06 deception-environment
contract family); a ``.devN`` suffix is only present between releases.
"""

__version__ = "0.5.0"
